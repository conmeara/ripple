#!/usr/bin/env node
// Cross-platform launcher for npm and plugin installs. Import resolution is
// relative to this real file even when npm exposes it through a .bin shim.
await import("../cli/index.mjs");
