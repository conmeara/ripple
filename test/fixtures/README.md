# Ripple Test Fixtures

This directory contains stable fixture projects for workflow-level tests and
release smokes. Fixtures should be small, deterministic, offline, and safe to
copy into temporary directories.

- `hyperframes/basic-title-card`: minimal valid HyperFrames project for preview
  and export smoke tests.

Do not put user content, external network assets, provider credentials, or
large generated outputs here.
